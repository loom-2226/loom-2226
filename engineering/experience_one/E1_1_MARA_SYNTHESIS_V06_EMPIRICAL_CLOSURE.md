# LOOM 2226 — E1.1 Mara Synthesis v0.6 Empirical Closure

**Status:** PASS / EMPIRICALLY_TESTED  
**Date:** 12 September 2026  
**Class:** `class:engineering` / `REQUIRED_FOR_E1`  
**Scope:** bounded read-only Mara synthesis over deterministic Ceres Canon Context Projection

## Result

Pixel/Termux execution of `engineering/experience_one/spikes/e1_1_mara_grounded_synthesis_v06.py` against the unchanged real projection artifact `E1_1_CERES_CONTEXT_V1.json` completed all four adversarial cases successfully.

Observed result:

- `why_restricted` — PASS — assessment `MIXED`, acceptable `['MIXED']`;
- `who_runs_it` — PASS — assessment `SUPPORTED`, acceptable `['MIXED', 'SUPPORTED']`;
- `unsupported_sabotage_cause` — PASS — assessment `NOT_ESTABLISHED`, acceptable `['NOT_ESTABLISHED']`;
- `contradictory_user_claim` — PASS — assessment `CONTRADICTED`, acceptable `['CONTRADICTED', 'MIXED']`.

Aggregate:

- `all_cases_pass: true`;
- input tokens: 5,602;
- output tokens: 965;
- audited provider entries written: 8;
- output artifact: `/storage/emulated/0/Download/E1_1_MARA_SYNTHESIS_RESULT_V06.json`.

## What this empirically establishes

Within this bounded E1.1 experiment, Mara can:

1. map ordinary user language to one typed read-only canon-context query;
2. resolve both full projected names and the deterministic context-qualified alias to canonical target `CER-P05`;
3. synthesize only from the deterministic tool packet;
4. cite packet-backed evidence paths;
5. preserve uncertainty when a comparison or causal claim is not established;
6. reject an unsupported sabotage narrative without inventing history;
7. reject a user instruction that conflicts with deterministic evidence;
8. distinguish supported, not-established, contradicted, and mixed epistemic conditions without receiving calculation, state, canon, or SQLite authority.

## Authority boundary retained

- model SQLite access = false;
- model calculation authority = ZERO;
- model state authority = ZERO;
- model canon authority = ZERO;
- tool path remains read-only;
- entity normalization remains deterministic and fail-closed;
- compact query remains the deterministic fact-selection boundary;
- evidence validation remains deterministic against the returned packet;
- no campaign mutation occurs;
- no WORLD/CIVSTATE mutation occurs.

## Interpretation

The earlier red runs were useful evidence rather than failed product behavior. They exposed, in sequence:

- a too-brittle model/entity-reference boundary;
- a harness that did not preserve partial failure evidence;
- an overloaded scalar epistemic-status contract;
- evidence validation that incorrectly assumed all legitimate evidence lived under `facts`;
- negation-blind string checks;
- overly narrow single-label expectations for compound questions.

The v0.6 result closes this bounded discriminator without prompt-tuning the model to memorize expected labels or weakening the deterministic authority boundary.

## Closure claim

**Mara grounded synthesis over the Ceres read-only Canon Context Projection is PASS / EMPIRICALLY_TESTED for the bounded four-case E1.1 adversarial set.**

This is not a claim that general conversational Mara is complete, that all world entities are projected, or that semantic correctness can be proven for arbitrary prose. It is evidence that the architecture can support grounded read-only conversational synthesis without granting the model world-state or calculation authority.

## Next seam

Do not continue polishing this harness unless a later regression breaks one of the proved boundaries. Move E1.1 attention to the next missing acceptance-context seam, while preserving this result as regression evidence.

Candidate next seams should be selected from the Experience One preregistered flow, with the same diagnostic precedence:

`DATA -> PROJECTION -> SYNTHESIS -> PRESENTATION -> MECHANICS -> MOTIVATION`.

The next likely read-only context need is to connect the interrogable-world path to the actual Experience One operator context (Wayfarer/current location and/or Neptune destination context) without allowing place context to masquerade as campaign-state authority.

## Falsifier

Reopen this closure if a regression permits nonexistent evidence paths, bypasses deterministic entity normalization, grants model SQLite/state/calculation/canon authority, fails closed incorrectly on the established aliases, or produces unsupported factual assertions in the bounded regression set.
