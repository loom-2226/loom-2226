# Earth long-run economic baseline v2 — 23 September 2026

**Disposition: promoted as the local Earth long-run economic reference scenario.** The selected, previously qualified run is `v0.6.1-d1-c1-h1-r1-alpha060-national-gfcf1`, with policy `WDI_NATIONAL_GFCF_GDP_LATEST_2024_2025_MEDIAN_2024_FALLBACK_v1`. `BASELINE_MANIFEST.json` identifies its exact code, source, annual output, endpoint and complete 2226 checkpoint through the immutable predecessor `RUN_MANIFEST.json`. The root `EARTH_LONG_RUN_ECONOMIC_BASELINE_CURRENT.json` designates this version. No runner, source, frozen state, model parameter, output or checkpoint was changed for this promotion; no trajectory was run.

This designation supersedes the **local** v1 Earth reference at `earth_baseline_2226_2026_09_23/`, which remains the rollback target with its original `BASELINE_MANIFEST.json`, report, outputs and checkpoint unchanged. The v1 policy is `UNTREATED_FROZEN_2060_EXPLICIT_v1` and has no alpha ceiling. The intervening alpha=0.60 sensitivity run under `alpha_ceiling_round1_2026_09_23/CAP_060/` remains a comparison artifact, not the former formally frozen baseline. No LOOM canon, WORLD, CIVSTATE, Atlas, protected repository branch or production runtime is promoted by this local designation.

The frozen 2026 seed and 2060 boundary are unchanged. From 2061 onward, this scenario applies the universal `alpha_effective = min(alpha_original, 0.60)` with a productivity rebase preserving 2060 production, and uses the pinned WDI national-account GFCF/GDP rate as the fixed country investment/value-added rate. There are 74 direct 2024–2025 rates and six same-series pooled-median imputations. The six recipients and each country's source year, method and fallback flag are recorded in `INVESTMENT_RATE_PROVENANCE.csv`. This is a source-corrected, numerically qualified **scenario**, not a validated 2226 forecast. In particular, the alpha ceiling, use of current-price national-account rates against inherited constant-2015-USD-scale model value added, fixed rates through 2226, and post-2100 authored demographics remain assumptions. The 80-economy aggregate is not the entire world.

The selected 2226 endpoint has 6,572,523,389.641058 modeled persons, 581,741,289,556,320.9 annual value-added proxy units, 3,287,021,023,572,331 capital-stock proxy units and 155,436,038,539,386.03 annual investment proxy units. The frozen 2060 country, sector and asset rows match the alpha=0.60 predecessor byte-for-byte. Recorded qualification includes 12 focused tests, four inherited regressions, no 2061–2226 annual-gate failures, zero 2190/2226 country–sector–asset aggregation residual, and exact six-file fresh-process checkpoint restoration at 2226. The historic v0.5 2032 mid-stage gate remains **REVIEW**; the separately scoped v0.2 boundary gate remains **PASS**. Promotion does not reinterpret either.

For reproduction, use a fresh isolated execution copy of the selected run directory and the exact inputs and commands in its `RUN_MANIFEST.json`. Output directories must be new. For a direct integrity check without rerunning the model, recompute the SHA-256 and byte size of each registered `inputs`, `code`, `outputs` and `evidence` entry and compare to that manifest, then compare its SHA-256 to this baseline manifest. Preserve the source paths or restore the same files with documented path redirection.

**Rollback:** the preserved v1 baseline is `earth_baseline_2226_2026_09_23/BASELINE_MANIFEST.json` (SHA-256 `c93452288de6a787518e26e65f6bb10fe0da600db161d14330ba4ddf8938088e`). Verify its file register first. To reverse this designation, atomically repoint only the root current-baseline metadata to that exact manifest and policy, recording the disposition; do not overwrite either run or its checkpoint. This promotion has not created an independent backup or tested restoration on another machine.
