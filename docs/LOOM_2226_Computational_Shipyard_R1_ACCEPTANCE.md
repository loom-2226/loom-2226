# LOOM 2226 — Semantic Geometry R1 Acceptance

R1 acceptance requires executable evidence that:

- semantic records map 1:1 to governed geometry primitives;
- governed source hashes remain unchanged;
- admitted envelopes, structural hypotheses and source-node proxies retain distinct semantics;
- structural hypotheses cannot become qualified by metadata;
- point-mass/source-node proxies cannot silently become volumes;
- subsystem identity remains OPEN unless governed upstream evidence exists;
- authority escalation fails closed;
- deterministic repeat builds produce identical semantic packages;
- existing governed-synthesis tests remain green;
- repository unit regression remains green before claiming regression-safe closure.

No visual realization generated downstream is engineering authority.
